spring:
  application:
    name: grammar-server

server:
  port: 8080

# Наружу открыт только health — остальные эндпоинты Actuator светят конфигом и бинами.
management:
  endpoints:
    web:
      exposure:
        include: health