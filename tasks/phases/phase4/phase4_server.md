# Фаза 4 — Сервер (Spring Boot, Java)

> Полный план серверной части. Общий контекст — в [grammar8r_plan.md](../../grammar8r_plan.md) и [subscription.md](../../subscription.md).
> 📝 **Перед стартом Фазы 4 прочитать [phase4_infra_draft.md](phase4_infra_draft.md)** — черновик
> обсуждения от 21.09.2026: словарный прокси и кэш переводов, TTL, Redis, число баз, железо,
> раскладка контейнеров, сайт с логином и оплатой, DDoS, мониторинг и Telegram-бот с алертами.
> Большая часть там **не согласована** — это запись диалога, к которому надо вернуться и
> переобсудить. Часть пунктов этого файла он помечает как устаревшие (см. его §15).
> ✅ **Согласовано 26.09.2026 и перенесено сюда решениями:** хостинг Beget VPS в РФ, железо, модель
> DeepSeek, без Redis (кэш слов в PostgreSQL), Caddy, бэкапы в Yandex Object Storage, снимки,
> проверка восстановления, алерты, восстановление сервера с нуля — раздел
> «Инфраструктура: хостинг, бэкапы, восстановление» ниже. В черновике у этих пунктов стоит статус «решено».
> **Решение 02.07.2026: стек сменён с Ktor/Kotlin на Spring Boot/Java.** Причина: код пишет Claude,
> ревьюит пользователь — Java-Spring он знает и может жёстко ревьюить (для слоя с деньгами и
> авторизацией способность владельца читать код важнее лёгкости Ktor). Упоминания Ktor по планам
> обновлены; функционально план не изменился.

---

## Стек и хостинг

- **Язык/фреймворк:** Java + Spring Boot (Spring Security для JWT, Actuator для метрик/health)
- **БД:** PostgreSQL. Redis **не ставим** — кэш переводов и счётчики лимитов живут в PostgreSQL
- **AI-модель:** DeepSeek `deepseek-flash` (V4.1-Flash), расчёт расходов — `subscription.md` → «Расчёт расходов»
- **Деплой:** Docker (образ у Spring ~200–300 MB) + GitHub Actions: влили ветку в `master` → Actions собирает образ и выкатывает на сервер
- **Хостинг:** **Beget VPS, Россия** — 4 ядра 3–3,3 ГГц, 6 ГБ RAM, 80 ГБ NVMe, канал 1 Гб/с, **2 040 ₽/мес**
- **Почему в РФ:** DeepSeek работает с российских IP (единственной причиной зарубежного хостинга был
  OpenAI, который блокирует Россию); закон 152-ФЗ требует хранить персональные данные россиян (почта,
  имя) на серверах в России; ниже задержка для пользователей; вебхуки YooKassa ближе
- **Веб-сервер спереди:** Caddy (сам получает и продлевает HTTPS-сертификаты)
- **YooKassa:** принимает вебхуки на любой URL

Подробно — раздел «Инфраструктура: хостинг, бэкапы, восстановление» ниже.

### ⚠️ Шероховатости Java-сервера с Kotlin-контрактом (НЕ ПРОЕБАТЬ при реализации)

`grammar-shared` ОСТАЁТСЯ на Kotlin (его подключает Android-клиент, DTO на kotlinx.serialization) —
Java-сервер использует эти классы напрямую (один JVM-байткод). Три обязательных пункта:

1. **Сериализация на сервере — ТОЛЬКО kotlinx, НЕ Jackson.** Первым делом при поднятии сервера
   зарегистрировать `KotlinSerializationJsonHttpMessageConverter` (и убедиться, что он ПЕРЕХВАТЫВАЕТ
   наши DTO раньше Jackson). Дефолтный Jackson молча проигнорирует `@SerialName` из DTO — контракт
   разъедется с клиентом без единой ошибки компиляции. Проверить интеграционным тестом: JSON ответа
   сервера содержит имена из `@SerialName` (`"tier": "free"`, не `"FREE"`).
2. **Конструирование Kotlin-DTO из Java — только позиционные аргументы.** Именованных аргументов и
   дефолтов из Java нет: два соседних `Int`-поля (`aiRequestsToday`, `aiDailyLimit`) можно перепутать
   местами, компилятор не заметит. Дисциплина: при каждом вызове конструктора DTO — инлайн-комментарии
   с именем поля (`/* taskText */ text, /* aiRequestsToday */ 5, ...`). Ревьюить эти места в первую очередь.
3. **Null-safety на границе.** Java может передать null в non-null параметр Kotlin-класса — упадёт
   в рантайме (`NullPointerException` из intrinsics), а не на компиляции. На входных данных —
   валидация до конструирования DTO.

---

## Инфраструктура: хостинг, бэкапы, восстановление (решено 26.09.2026)

> Всё в этом разделе согласовано с пользователем 26.09.2026. Откуда что взялось и какие варианты
> отбросили — `phase4_infra_draft.md` (пункты там помечены «решено»).
> 🤝 **На старте Фазы 4 Claude помогает пользователю настроить всё это руками по шагам** — чек-лист
> в конце раздела. Пользователь попросил не забыть об этом напомнить.

### Сервер и почему его хватает

**Beget VPS, младший из двух рассмотренных тарифов:** 4 ядра 3–3,3 ГГц · 6 ГБ RAM · 80 ГБ NVMe · 1 Гб/с ·
**2 040 ₽/мес**. Старший (6 ядер, 12 ГБ, 150 ГБ, 3 600 ₽) — не нужен, пока не упрёмся (см. «Когда расти»).
Пользователь уже работал с Beget, претензий не было. Timeweb отброшен — его последнее время часто DDoS-ят.

**Прикидка нагрузки на 1 000 пользователей:**

- Сервер почти ничего не считает сам: главная работа — переслать запрос в DeepSeek и **подождать ответ**
  (3–10 с). Ожидание процессор не грузит — Spring Boot держит сотни таких «висящих» запросов.
- Запросов к ИИ в день: в среднем ~10–15 на человека (бесплатные меньше, платные больше) →
  **~10–15 тыс. в день**.
- **Пик — вечер по Москве (19–23).** Примерно половина пользователей живёт в МСК-поясе, вторая половина
  — в других поясах и размазывает нагрузку по суткам. В самый тяжёлый час приходит ~12–15% дневного
  объёма → ~1 500–2 000 запросов в час → **в среднем ~0,5 запроса в секунду, всплески до ~5 в секунду**.
- Одновременно висящих запросов к ИИ: в среднем ~5, на всплеске ~30 — для сервера это ерунда.
- Остальное — копейки: вход, синк прогресса, лимиты, стрик. Теория и `content.db` раздаются не с этого
  сервера, а из объектного хранилища (`phase4_content_delivery.md`).

**Память (6 ГБ):**

| Что | Примерно |
|---|---|
| Spring Boot (JVM) | ~0,5–1,5 ГБ |
| PostgreSQL | ~1–2 ГБ |
| Caddy | ~0,05 ГБ |
| Мониторинг, бот | ~0,5 ГБ |
| Docker + ОС | ~0,5 ГБ |
| **Итого** | **~3–4 ГБ из 6** |

**Диск (80 ГБ):** база с прогрессом 1 000 человек — единицы гигабайт. Опасность для диска — не данные, а
мусор: неротируемые логи и старые Docker-образы (образ Spring ~200–300 МБ × двадцать деплоев = 5 ГБ).
С первого дня: `logrotate` и `docker system prune` по расписанию.

**Итог:** на 1 000 человек сервер загружен на ~10–20%, по ощущению потянет 3–5 тысяч. Упрёмся скорее
в лимиты и скорость ответа DeepSeek, чем в сервер. По деньгам: 2 040 ₽ на 1 000 человек ≈ 2 ₽ на
человека в месяц, окупается ~15–20 подписчиками.

**Когда расти.** VPS увеличивается кнопкой у Beget, минуты простоя. Переходить на старший тариф — когда
загрузка процессора или памяти **стабильно** держится выше ~60–70% (покажет мониторинг), либо когда на ту
же машину сядут тяжёлые вещи (свой Grafana-стек и т.п.). Не заранее.

### Что крутится на сервере

```
caddy      — HTTPS (сертификаты Let's Encrypt получает и продлевает сам), раздаёт сайт, проксирует /api и /admin
app        — Spring Boot: API + админка
postgres   — все данные; volume наружу, порт наружу НЕ публикуется (в compose через expose, не ports)
```

- **Caddy, а не nginx** — сертификаты без certbot и крона, конфиг в несколько строк. В нашем масштабе
  плюсы nginx не нужны.
- **Redis не ставим.** Всё, ради чего его обычно берут, у нас делает PostgreSQL:
  - **кэш переводов слов** — обычная таблица «слово → перевод» с индексом по слову, поиск — доли
    миллисекунды; плюс на телефоне своя копия кэша (`DictionaryCache` в Room), и уже смотренное слово на
    сервер вообще не идёт;
  - **лимиты, счётчики запросов, стрики** — обычные строки в базе.

  На нашей нагрузке (единицы запросов в секунду) база этого не заметит. Redis нужен при десятках тысяч
  одновременных пользователей или очень плотной защите от спама запросами — не наш случай. Если
  когда-нибудь упрёмся — сначала кэш внутри самого Spring (Caffeine, `@Cacheable`), и только потом Redis.
- Каждому сервису `mem_limit`, `restart: unless-stopped`; данные Postgres — на именованном volume
  (`docker compose down -v` не должен уносить базу).
- **Образ собирается в GitHub Actions, а не на сервере** — Gradle-сборка на самой машине съест память и
  уронит работающее.

### Бэкапы базы — в Yandex Object Storage

**Что такое дамп.** Полная копия базы PostgreSQL одним файлом (`pg_dump`): аккаунты, прогресс, стрики,
лимиты, платежи и подписки, промты (они в таблице `ai_exercise_prompts`) и кэш переводов слов. На 1 000
активных пользователей — **десятки мегабайт**, после сжатия в разы меньше.

- **Кэш переводов в дампе оставляем**, пока он не разрастётся: после восстановления не придётся заново
  ходить в платный словарь за уже известными словами. Разрастётся — исключить содержимое этой таблицы
  из дампа одной опцией `pg_dump` (кэш сам наполнится заново).

**Куда.** В **Yandex Object Storage** — тот же аккаунт Яндекса, что и CDN для раздачи контента, но
главное: **это другая компания, не Beget.** Если с Beget что-то случится (падение, блокировка аккаунта,
ошибка), копии останутся целы. Хранилище стоит копейки — файлы мегабайтные.

**Как и когда.** Автоматически, без участия человека (не «скачивать на свой комп раз в несколько
дней» — комп может быть выключен, а потеря нескольких дней прогресса всех пользователей недопустима):

1. **Раз в сутки ночью**, в 04:00 по Москве — когда меньше всего нагрузки (можно и два раза в сутки,
   это копейки).
2. `pg_dump` → сжать → **зашифровать** → отправить в Yandex Object Storage.

**Сколько храним — схема 7 / 4 / 3:**

| Копии | Сколько | Зачем |
|---|---|---|
| Ежедневные | 7 | откатиться на любой день последней недели |
| Еженедельные | 4 | если беду заметили через 2–3 недели |
| Ежемесячные | 3 | на совсем крайний случай |

**Почему не просто «последние 10».** Самая частая беда — не падение сервера (от него хватит и последней
копии), а **тихая порча данных**: выкатили обновление с багом, и оно неделю незаметно портит, скажем,
стрики. Заметили через 8 дней — а «последние 10 копий по две в день» покрывают всего 5 дней, и все они
уже с испорченными данными, откатиться некуда. Схема 7/4/3 — это ~14 файлов вместо 10, а назад
дотягиваемся на 3 месяца. Старые файлы Яндекс удаляет сам по правилам хранения (lifecycle) — руками
ничего чистить не нужно.

**Ключ шифрования — в трёх местах:** на сервере (им шифрует скрипт бэкапа), в секретах GitHub и на
компе пользователя — **в менеджере паролей или зашифрованной заметке, не в txt на рабочем столе**.
Без ключа бэкап — мусор: если сервер слетит вместе с ключом, а копий ключа нигде нет, бэкапы не открыть.

**Скачивать дампы на свой комп регулярно не нужно** — основная копия в Яндексе. Качаем только в день
беды, чтобы развернуть, и раз в месяц для проверки (ниже).

**Что можем потерять при сбое:** максимум сутки (с последнего ночного дампа). Частично прикрыто тем,
что прогресс хранится и на телефонах (`user.db`) и досинхронизируется. Когда пользователей станет
много — перейти на непрерывное копирование журнала базы (`wal-g` / `pgBackRest`), потери сократятся до минут.
На старте ночного дампа достаточно.

### Снимки диска у Beget — вторая линия

- Beget **бесплатно** сам делает копии файлов VPS раз в несколько дней.
- **Снимки диска (snapshot)** — 5 ₽ за ГБ в месяц, платим за **занятое** место, а не за все 80 ГБ: у нас
  несколько гигабайт → десятки рублей в месяц. Снимок откатывает весь сервер целиком за минуты.
- **Держим один свежий снимок постоянно** + **делаем ручной перед рискованными делами** (обновление
  системы, изменение структуры базы, крупный деплой), после успешного дела — удаляем.
- ⚠️ Снимок лежит у того же Beget — он **дополняет** бэкап в Яндексе, а не заменяет его.

### Проверка восстановления — раз в месяц

Непроверенный бэкап — это надежда, а не бэкап. Раз в месяц (~15–20 минут):

1. Скачать свежий дамп из Яндекса.
2. Расшифровать ключом.
3. Поднять у себя в Docker PostgreSQL + сервер, залить дамп (`pg_restore`).
4. Зайти в приложение на локальный сервер и убедиться, что данные на месте.

Так в день беды точно известно, что восстановление работает и какие команды вводить.

### Алерты в Telegram

- **Бэкап не прошёл → громкое сообщение в Telegram.** Пусть будит ночью — или утром видно, что
  случилось, и идём чинить.
- **Раз в неделю — короткое «бэкапы в порядке, последний такой-то».** Иначе, если сломается сам бот,
  не узнать, что он молчит не потому, что всё хорошо.
- Это тот же бот, что и для остальных алертов (см. `phase4_infra_draft.md` → п. 13).

### Сервер восстанавливается с нуля — всё нужное лежит не только на нём

Правило: всё, что нужно, чтобы поднять сервер заново, должно храниться **не только на самом сервере**.

- **Код и настройки — в git** (актуальная версия — ветка `master`): код сервера, `docker-compose.yml`,
  `Caddyfile`, скрипт бэкапа, workflow GitHub Actions.
- **Секреты — в секретах GitHub И копией в менеджере паролей пользователя:** ключ DeepSeek, пароль базы,
  `JWT_SECRET`, ключи ЮKassa, пароль админки, ключ шифрования бэкапов, ключи доступа к Yandex Object
  Storage, ssh-ключ к серверу, токен Telegram-бота.

**Если сервер потерян целиком (≈1 час):**

1. Взять новый VPS у Beget.
2. Запустить деплой через GitHub Actions — сервер поднимется из `master`.
3. Скачать последний дамп из Yandex Object Storage, расшифровать, залить (`pg_restore`).
4. Проверить, что приложение работает.

### 🤝 Чек-лист «настроить вместе» на старте Фазы 4

- [ ] Beget VPS 2 040 ₽ — заказать, настроить доступ по ssh-ключу
- [ ] Docker + `docker-compose.yml` (caddy / app / postgres), `mem_limit`, volume, `expose` для Postgres
- [ ] Caddy: домен, HTTPS, проксирование `/api` и `/admin`, закрыть `/actuator/**` (кроме health)
- [ ] GitHub Actions: сборка образа и деплой при вливании в `master`, секреты в GitHub
- [ ] Yandex Object Storage: бакет под бэкапы, правила хранения 7/4/3
- [ ] Скрипт ночного бэкапа (04:00 МСК): `pg_dump` → сжать → зашифровать → в Яндекс
- [ ] Ключ шифрования — в три места (сервер, GitHub, менеджер паролей)
- [ ] Telegram: алерт о неудачном бэкапе + еженедельное «всё в порядке»
- [ ] Снимок диска Beget — первый свежий
- [ ] `logrotate` и `docker system prune` по расписанию
- [ ] Первая проверка восстановления в локальном Docker
- [ ] Спросить у Beget, что входит в защиту от DDoS

---

## Эндпоинты

### Авторизация

```
POST /auth/register
  Body: { email, password }
  → { uid, accessToken, refreshToken }
  Логика: проверить email уникальность, bcrypt пароль, создать uid, выдать оба токена

POST /auth/login
  Body: { email, password }
  → { uid, accessToken, refreshToken }
  Логика: найти по email, проверить bcrypt, выдать оба токена

POST /auth/yandex
  Body: { yandexToken }
  → { uid, accessToken, refreshToken }
  Логика: верифицировать токен у Яндекса, получить yandex_uid,
          найти или создать пользователя, выдать оба токена

POST /auth/refresh
  Body: { refreshToken }
  → { accessToken, refreshToken }
  Логика: проверить refreshToken в БД → если валиден:
          1. Прочитать актуальный tier из таблицы subscriptions
          2. Выдать новый accessToken (с актуальным tier внутри)
          3. Выдать новый refreshToken (ротация — старый инвалидируется)
          Если невалиден → 401
```

**Access token** — живёт **1 час**. Содержит: `{ uid, tier, exp }`. Сервер верифицирует подпись без запроса к БД — tier читается прямо из токена.

**Refresh token** — живёт **30 дней**. Хранится в таблице `refresh_tokens` в БД. Используется только для получения нового access token. При каждом /auth/refresh — ротация: старый удаляется, выдаётся новый.

**Итог:** tier актуален с погрешностью до 1 часа (время жизни access token). Это приемлемо — при покупке подписки клиент сразу вызывает /auth/refresh чтобы получить токен с новым tier.

### Подписка

```
GET /subscription
  Headers: Authorization: Bearer <jwt>
  → {
      tier: "free" | "tier1" | "tier2" | "tester",
      expires: "2026-05-01" | null,
      aiRequestsToday: 2,
      aiDailyLimit: 3,
      microtopicsToday: 1,
      microtopicsDailyLimit: 5 | null,   ← null = безлимит
      streakCurrent: 14,
      streakBonusRequests: 1
    }
  Лимиты берутся из таблицы subscription_limits по tier пользователя (кэш TTL ~5 мин).

GET /tier-limits
  Публичный, без авторизации.
  → {
      free:  { aiPerDay: 3,  microtopicsPerDay: 3,  wordsInPrompt: 50  },
      tier1: { aiPerDay: 30, microtopicsPerDay: null, wordsInPrompt: 100 },
      tier2: { aiPerDay: 50, microtopicsPerDay: null, wordsInPrompt: 150 }
    }
  Значения берутся из subscription_limits (кэш TTL ~5 мин). Тир tester не включается.
  Используется на маркетинговом экране подписки — показывает актуальные лимиты без авторизации.

```

### Оплата и промокоды

```
POST /promo/validate
  Headers: Authorization: Bearer <jwt>
  Body: { code: "ABCD-1234" }
  → { valid: true, discountPercent: 25, finalPrice: { tier1: 75, tier2: 134 } }
  | { valid: false, reason: "not_found" | "expired" | "used_up" }
  Логика: найти промокод → проверить expires_at и used_count < max_uses → вернуть скидку.
  Промокод не списывается здесь — только при успешной оплате через вебхук.

POST /payment/create
  ⚠️ Схема ниже — для RuStore-сборки (оплата в приложении). Для Play/iOS оплата идёт на сайте
     под тем же аккаунтом, а подписка вычисляется из платежей с разными источниками
     (yookassa_app / yookassa_web / apple_iap / manual) — см. subscription.md → «Платёжные каналы
     по площадкам» и ios_portability_plan.md §8.5. Учесть при проектировании таблицы подписок.
  Headers: Authorization: Bearer <jwt>
  Body: { tier: "tier1" | "tier2", promoCode?: "ABCD-1234" }
  → { paymentUrl: "https://yookassa.ru/..." }
  Логика:
    1. Если promoCode передан — проверить валидность (те же проверки что в /promo/validate)
    2. Рассчитать итоговую цену со скидкой (если есть промокод)
    3. Создать платёж в YooKassa через их API: сумма, описание, metadata: { uid, tier, promoCode? }
    4. Вернуть клиенту URL страницы оплаты — клиент открывает WebView/браузер
  ⚠️ Цену устанавливает только сервер — клиент не может передать сумму.

POST /webhook/yookassa
  ← Вебхук от YooKassa об успешной оплате
  Логика: верифицировать подпись YooKassa → из metadata достать uid, tier, promoCode?
          → обновить tier и expires в subscriptions
          → если promoCode есть → инкрементировать promo_codes.used_count
          → клиент узнаёт о смене тира при следующем /auth/refresh
```

### AI-прокси

```
POST /ai/exercise/generate
  Headers: Authorization: Bearer <jwt>
         | X-Device-Id: <deviceId>   ← если не залогинен, tier = Free принудительно
  Body: { exerciseId, words[] }
  ← exerciseId = AiExercise.id — сервер находит промт в ai_exercise_prompts
  ← words[] — клиент собирает сам по WordSource из wordsSource упражнения
  → { taskText }              ← сгенерированное задание, клиент показывает пользователю
  | { error: "limit_exceeded" | "ai_error" | "prompt_not_found" }
  ⚠️ Лимит daily_ai_requests списывается здесь — на генерации, не на проверке.

POST /ai/exercise/evaluate
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Body: { exerciseId, taskText, userAnswer }
  ← taskText — то что вернул /generate, клиент хранил в ViewModel
  ← userAnswer — ответ пользователя
  → { isCorrect, correctedAnswer, feedback }
  | { error: "ai_error" }
  ⚠️ Баллов нет (решение 20.09.2026): пользователь видит только «верно / неверно» + фидбек.
  ⚠️ Лимит НЕ списывается. Стрик обновляется здесь (пользователь реально выполнил задание).

POST /ai/clarification
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Body: { cardId, cardTheory, userQuestion }
  → { answer, aiRequestsToday, aiDailyLimit }
  | { error: "limit_exceeded" | "ai_error" }
```

### AI-диалог (режим практики «Диалог»)

> Контекст и UX — `practice_plan.md` → «Режим: Диалог». Здесь — серверная механика хранения сессии и потока обмена репликами.
>
> **Главный принцип:** в отличие от остальных AI-режимов (одношаговые: запрос → ответ), диалог — многошаговый и стейтфул. История переписки и срез слов пользователя хранятся **на сервере** в одной сессии, фиксируются один раз при старте — клиент при каждом обмене шлёт только новую реплику + `sessionId`, не пересылая ни историю, ни слова заново.

```
POST /ai/dialog/start
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Body: { difficulty: "simple" | "medium" | "hard", words[] }
  ← words[] — срез слов пользователя, клиент собирает один раз по алгоритму выборки
  → { sessionId, reply, translation, expiresAt }
  | { error: "limit_exceeded" | "ai_error" }
  Логика:
    1. Сформировать system_prompt: фиксирует уровень сложности + срез слов (на весь диалог, неизменно)
    2. Создать запись в ai_dialog_sessions (history = пустой массив, turn_count = 0, expires_at = now + 1 час)
    3. Запросить у LLM открывающую реплику (первый вопрос соответствующего уровня)
    4. Записать реплику в history, вернуть клиенту { sessionId, reply, translation, expiresAt }
  ⚠️ Лимит daily_ai_requests списывается здесь — это первый AI-запрос сессии (генерация открывающей реплики)

POST /ai/dialog/message
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Body: { sessionId, userMessage }
  → { feedback: { hasErrors, errors[] }, reply, translation, turnCount }
  | { isFinal: true, summary }                         ← если turnCount достиг 5 — диалог завершён
  | { error: "session_not_found" | "session_expired" | "limit_exceeded" | "ai_error" }
  Логика:
    1. Загрузить сессию по sessionId; если expires_at < now → удалить, вернуть session_expired
    2. Собрать полный промт = system_prompt (из сессии, без изменений) + history + userMessage
    3. Один запрос к LLM — модель одновременно оценивает грамматику userMessage
       (errors[] с original/correction/explanation) И формирует следующую реплику с учётом
       уровня сложности и контекста беседы — единый JSON-ответ { feedback, reply, translation }
    4. Дописать обмен (userMessage + ответ ИИ) в history, turn_count += 1
    5. Если turn_count == 5 → запросить у LLM финальную сводку по всему диалогу,
       удалить сессию, вернуть { isFinal: true, summary }
    6. Иначе сохранить сессию, вернуть { feedback, reply, translation, turnCount }
  ⚠️ Лимит списывается на каждый вызов — один LLM-запрос даёт сразу фидбек ПРЕДЫДУЩЕЙ реплики
     и саму следующую реплику, поэтому 1 обмен = 1 единица лимита, не 2

POST /ai/dialog/end
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Body: { sessionId }
  → { ok: true }
  Логика: немедленное удаление сессии по кнопке [Завершить диалог] на экране клиента —
          без сводки, невзирая на expires_at. Лимит не списывается и не возвращается.
```

**Формат ответа LLM на каждый обмен** (то, что сервер просит у модели и парсит из её ответа):
```json
{
  "feedback": {
    "hasErrors": true,
    "errors": [
      { "original": "I goes there", "correction": "I go there", "explanation": "I — не 3-е лицо, окончание -s не нужно" }
    ]
  },
  "reply": "That's interesting! How often do you go there?",
  "translation": "Интересно! Как часто ты туда ходишь?"
}
```
Если ошибок не было — `hasErrors: false`, `errors: []`. `translation` — перевод реплики ИИ на русский, чтобы вопрос/реакция ИИ не были барьером для понимания.

**Очистка просроченных сессий:** фоновая задача удаляет записи `ai_dialog_sessions` где `expires_at < now` — раз в несколько минут (например, каждые 5). Это покрывает случай «бросил диалог на середине» — сессия исчезает сама, без сводки.

### Лимиты (для микротем — трекинг на сервере)

```
POST /limits/microtopic
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Body: { microtopicId }
  → { allowed: true, microtopicsToday, microtopicsDailyLimit }
  | { allowed: false, resetAt: "2026-04-24T00:00:00+03:00" }
  Логика: проверить лимит микротем, инкрементировать если разрешено
```

### Стрик — активность

```
POST /streak/activity
  Headers: Authorization: Bearer <jwt>
  Body: {
    source: "grammar8r" | "words8r",
    activityType: "ai_exercise" | "repetition" | "wordly" | "maze" | "new_words"
  }
  → {
      streakIncremented: bool,   ← true = стрик вырос сегодня, false = уже засчитано ранее
      currentStreak: Int,
      daysToNextMilestone: Int,  ← сколько дней до следующего milestone (0 если только что достигнут)
      nextMilestone: Int,        ← номер следующего milestone (7/20/30/60/100)
      nextMilestoneBonus: String ← "+2 запроса/день" — для отображения в баннере
    }

  Логика:
    1. Проверить last_activity_msk == today_msk? → streakIncremented: false, вернуть текущие данные
    2. Иначе → применить streak_logic (продолжить/сбросить/заморозка)
    3. Вернуть актуальные данные стрика

  Вызывается:
    - Grammar8r: параллельно с POST /ai/exercise (не ждём ответа перед показом фидбека AI)
    - Words8r: после завершения сессии/игры
```

**Важно:** шаг 9 в потоке `POST /ai/exercise` ("обновить стрик") теперь вынесен в отдельную функцию `streak_logic()`, которая используется и здесь, и там. Не дублировать логику.

### Синхронизация прогресса

```
POST /progress/sync
  Headers: Authorization: Bearer <jwt>
  Body: { events: [{ type: "card_completed" | "exercise_done", entityId, timestamp }] }
  → { ok: true }
  Логика: сохранить события прогресса, офлайн-буфер на клиенте улетает пачкой при появлении сети

GET /progress
  Headers: Authorization: Bearer <jwt>
  → { completedCards: [id...], dailyStats: {...}, streakData: {...} }
  Логика: вернуть весь прогресс пользователя — используется при переустановке/смене телефона
```

### Голоса озвучки (TTS-модели)

Список голосов и превью MP3 захардкожены в APK — сервер их не отдаёт.
Сервер только раздаёт файлы моделей когда пользователь выбирает платный голос.

```
GET /voices/{id}/model.onnx
  Headers: Authorization: Bearer <jwt>
  Логика: проверить что tier пользователя = tier1 или tier2. Если free → 403.
  → бинарный файл модели (.onnx)

GET /voices/{id}/tokens.txt
  Headers: Authorization: Bearer <jwt>
  Логика: та же проверка подписки
  → текстовый файл токенов
```

Бесплатный голос (`hfc_male`) бандлен в APK — для него запросы на сервер не нужны.
Файлы моделей хранятся на сервере в статике или объектном хранилище.
Клиент скачивает выбранный голос в `filesDir/voices/<id>/` и при следующем запуске грузит оттуда.

### Объявления (баннеры и push)

```
GET /announcements
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Query: app=grammar8r | words8r
  → { announcements: [{ id, message, type: "modal" | "push", createdAt, expiresAt }] }
  Логика: вернуть только активные (now < expires_at). Приложение вызывает при каждом старте.
  Клиент хранит локально id показанных modal-объявлений — не показывать повторно.
```

### Новости (лента постов) 🔜 БЛИЖЕ К РЕЛИЗУ

> Зафиксировано 05.09.2026. Делаем **до релиза, под конец разработки**, вместе с остальной
> серверной частью. ⚠️ Не жёсткая спека: эндпоинты, поля и схема ниже — **черновик направления**,
> при реализации почти наверняка изменятся. Продуктовая спека (экран, границы каналов, свобода
> комбинаций) — `features.md` → «Новости приложения».

Причина существования: карточки и микротемы приезжают доставкой контента **без обновления
приложения** (`phase4_content_delivery.md`) — без ленты пользователь о добавленном не узнаёт.

```
GET /news
  Headers: Authorization: Bearer <jwt> | X-Device-Id: <deviceId>
  Query: app=grammar8r | words8r
  → { posts: [{ id, title, body, imageUrl?, createdAt, updatedAt }] }
  Логика: только не истёкшие (now < expires_at), новые сверху.
  Прочитанность НЕ на сервере — клиент держит id прочитанных в DataStore.
```

**Одна рассылка — три независимых канала.** `POST /admin/news` принимает пост и опционально
поднимает соседние каналы одной операцией; тексты у каналов **свои**, не размноженный один:

```
POST /admin/news
  { title, body, imageUrl?,                    ← сам пост (обязателен для этого эндпоинта)
    banner?: { message, imageUrl? },           ← создать ещё и modal-объявление (TTL 24ч)
    push?:   { text } }                        ← разослать ещё и FCM-пуш
```

- Баннер и пуш **без поста** отправляются как раньше — через `/admin/announcements`
  и `/admin/users/{uid}/notify`; новый эндпоинт их не заменяет и не поглощает.
- Пуш **неотзываем**: `PATCH`/`DELETE` поста его не трогают и не переотправляют.
- Если у рассылки есть и пост, и баннер/пуш — те несут `newsPostId` для перехода в ленту.

**Хранение:** `expires_at = created_at + 60 дней`. Медиа — в том же объектном хранилище,
что и контент (секретов не держит, раздача статики); в БД лежит только URL.

### Векторные анимации (Lottie) 🔜 БЛИЖЕ К РЕЛИЗУ

> Продуктовая спека, правила и рабочий процесс — `features.md` → «Векторные анимации (Lottie)».
> Здесь только серверная часть. ⚠️ Черновик, поля изменятся.

Анимация = обычный файл в объектном хранилище (`animations/{id}.json`), раздаётся как статика —
кода и эндпоинтов для неё не нужно. Клиент получает URL двумя путями:

1. **В рассылке** — поля `imageUrl` поста и баннера (см. выше) принимают и Lottie-JSON.
2. **В каталоге анимаций приложения** — для встроенных мест (завершение карточки, стрик +1,
   завершение микротемы, сезонное оформление профиля):

```
GET <storage>/animations/catalog.json
  → { version, items: [{ slot, url, weight, from?, to?, minAppVersion? }] }
  slot   — где играет (enum на клиенте: CARD_DONE / STREAK_UP / MICROTOPIC_DONE / PROFILE_SEASON …)
  from/to — окно показа (для сезонных: снежинки только зимой)
```

#### ⭐ Где что лежит: «сервер раздаёт смысл, хранилище — байты» (решение 05.09.2026)

**Все файлы анимаций — в объектном хранилище. ОБА класса, включая одноразовые баннерные.**
Боевой сервер к раздаче медиа не подпускаем вообще.

Причина: анимация — статический иммутабельный файл, одинаковый для всех, только на чтение, то
есть ровно тот профиль, ради которого хранилище и заводилось. То, что баннерная анимация
проиграется один раз, её природы не меняет. А раздача с боевого сервера собрала бы все минусы,
от которых мы ушли в начале этого файла: узел **за рубежом** (лишняя задержка и риск блокировки
для наших пользователей), на нём **все секреты** (ключ DeepSeek, JWT, YooKassa), и бинарный трафик
начал бы конкурировать с оплатой и AI. Плюс два пути загрузки = две формы в панели, два
предпросмотра, два кэша и вечное «почему тут превью работает, а там нет».

Разделение проходит не по месту хранения, а по ответственности:

| | Кто раздаёт | Что именно |
|---|---|---|
| **Смысл** — что, кому и когда показать | боевой сервер | запись поста/баннера с полем URL; таргетинг; TTL; персональность |
| **Байты** — сам файл | объектное хранилище | `animations/{id}.json` + `animations/catalog.json` |

- Префикс `animations/` — **отдельный от `content/`**, чтобы бампы версии контента их не задевали.
- Уборка по той же линии: каталожные — снял из каталога, клиент сносит при уборке; одноразовые —
  файл удаляется тем же действием панели, что удаляет пост.

⚠️ **Мутабелен только каталог.** Файлы анимаций иммутабельны → кэш на год, больше никогда не
перекачиваются. `animations/catalog.json` — единственная изменяемая точка, ему короткий кэш.
Это же и есть причина правила «замена только новым именем файла».

⚠️ **Версия каталога анимаций НЕЗАВИСИМА от версии контента.** Момент проверки общий (удобно —
один поход при старте), но счётчики раздельные: иначе добавление анимации к баннеру заставит всех
перекачивать контент, а выкладка новой темы — трогать анимации.

⚠️ **Удаление по воздуху — требование, а не опция.** Анимация снимается с раздачи (убрана из
каталога / удалён файл) → клиент **обязан** молча перейти на фолбэк без неё, не показывая
ни дыры, ни ошибки. Неизвестный клиенту `slot` игнорируется — иначе новая анимация уронит
старые версии приложения.

⚠️ Потолок 300 КБ на файл. Кэш — как у контента (файл иммутабелен, обновление через новый URL
или бамп версии каталога).

**Два класса анимаций — сервер обязан различать** (подробно: `features.md`):

| | Каталожные | Одноразовые |
|---|---|---|
| Где объявлена | `catalog.json` | поле медиа в посте/баннере |
| Кто удаляет | убрал из каталога → уборка на клиенте | умирает с рассылкой (закрытие / TTL) |
| Примеры | завершение карточки, стрик +1, завершение микротемы, сезонный профиль | крест в баннере бана, иллюстрация к посту |

**Поля элемента каталога — ровно четыре условия, больше не заводить:**

| Поле | Смысл |
|------|-------|
| `slot` | Куда играет. Enum на клиенте; неизвестное значение игнорируется. |
| `weight` | Вес случайного выбора, если на слот повешено несколько вариантов. |
| `from` / `to` | Окно показа (сезонные: снежинки зимой, солнце летом). Null = всегда. |
| `minAppVersion` | Не отдавать клиентам, которые не умеют показать. |

⚠️ Отбор на клиенте: фильтр по условиям → случайный по весам → играть. **Движка правил в
клиенте не будет.** Понадобится сложное условие — фильтрует сервер и отдаёт готовый список.

⚠️ **Замена — только новым файлом.** Перезаписывать существующий URL запрещено: у пользователей
и в кэшах останется старая версия. Версионируем имя (`card_done_v2.json`), в каталоге меняем
ссылку.

### Панель: раздел «Анимации» 🔜 БЛИЖЕ К РЕЛИЗУ

Одно место, где видно и управляется всё хозяйство анимаций (черновик состава — уточняем при
реализации):

**Сводка сверху** — что где играет прямо сейчас: список слотов, сколько вариантов повешено на
каждый, какие сейчас активны по окну дат, суммарный вес хранилища, сколько анимаций «висит
мёртвым грузом» (загружены, но не привязаны ни к слоту, ни к рассылке).

**Действия:**
- **Загрузить анимацию** — файл + предпросмотр **сразу в нашей раме и палитре**, как увидит
  пользователь. Проверка потолка 300 КБ и валидности JSON на загрузке, а не «потом на телефоне».
- **Повесить на слот** — выбрать слот, задать `weight`, окно `from`/`to`, `minAppVersion`.
- **Заменить** — подставить новый файл в слот (старый остаётся в хранилище до явного удаления,
  чтобы можно было откатиться).
- **Снять со слота** — перестаёт отдаваться, клиенты переходят на фолбэк и сносят файл при уборке.
- **Удалить из хранилища** — насовсем. С предупреждением, если файл ещё где-то используется.
- **Отправить с рассылкой** — прикрепить анимацию к посту/баннеру как одноразовую (см. раздел
  «Новости»), не заводя её в каталог.

⚠️ Предпросмотр обязателен на каждом из путей: и для каталожной анимации, и для прикреплённой
к рассылке. Отправка вслепую недопустима — косяк иначе прилетает от пользователей.

⚠️ **Визуальная часть (вёрстка баннера с картинкой, карточка поста, форма в панели) — TBD**,
прорабатывается отдельно перед реализацией.

### Push-уведомления: как доставляются (зафиксировано 05.09.2026)

**Поток:** панель (браузер) → **наш сервер** → **FCM** → устройства.

Сервер проверяет права, достаёт токены из своей БД, формирует сообщение и делегирует доставку
Google. Google не знает ни про посты, ни про пользователей — видит только токен и короткий текст.

⚠️ **Панель НИКОГДА не ходит в FCM напрямую.** Ключ сервис-аккаунта живёт только на сервере;
обращение к Google из браузера означало бы утечку ключа в клиент.

**Решение по охвату: только FCM.** Устройства без сервисов Google (Huawei и часть RuStore-аудитории)
сознательно вне охвата — приложение пойдёт и в Google Play, где GMS есть у всех. RuStore Push /
Universal Push SDK **не подключаем**: это вторая система со своими условиями (установленный
RuStore, авторизация в нём, разрешённый фон, совпадение отпечатка подписи), а выигрыш не окупает
сложности. Пересмотреть только если статистика после запуска покажет заметную долю без GMS —
переделка будет заменой слоя отправки, не логики, потому что действует правило ниже.

⭐ **ГЛАВНОЕ ПРАВИЛО: пуш — это будильник, а не содержимое.** Пуш несёт короткий текст и максимум
id («есть новость, загляни»); правда всегда тянется с нашего сервера при открытии приложения.
Причина: доставка пуша **принципиально ненадёжна** — уведомления отключены, токен протух, Doze
задержал, система придушила частоту. Если новость или баннер зависят от прихода пуша, часть людей
не увидит их никогда. При правильной схеме непришедший пуш не теряет ничего.

**Формат сообщения:** notification + data. Систему просим показать уведомление самой (в фоне
отображение гарантировано), в `data` кладём id поста для перехода по тапу.

**Требования к панели (все согласованы 05.09.2026):**

1. **Тест-отправка себе** — кнопка «отправить на моё устройство» до массовой рассылки.
   У пользователя есть отдельный тестовый телефон. Пуш неотзываем, поэтому увидеть его глазами
   заранее обязательно.
2. **Подтверждение перед массовой** — экран «отправляется N людям, отозвать нельзя».
3. **Лог отправок** — что, когда, скольким ушло, сколько ошибок.
4. **Автоочистка мёртвых токенов** — FCM ответил `UNREGISTERED`/невалидный токен → сразу удаляем
   из БД. Иначе за год таблица превратится в помойку, а статистика доставки — во враньё.
5. **Каналы уведомлений** (Android Notification Channels): «Новости», «Стрик», «Технические».
   Человек должен уметь отключить напоминания про стрик, не отключая всё остальное — без каналов
   выбор бинарный, и он выключит всё.

⚠️ **Разрешение на уведомления (Android 13+)** спрашивается явно и **только один раз** — отказал,
дальше только через системные настройки. Поэтому спрашиваем **не на первом запуске** (незнакомое
приложение сразу что-то требует → отказ), а когда польза очевидна: после первого засчитанного дня
стрика, с одной фразой зачем.

### Веб-панель администратора

Простой веб-интерфейс для управления проектом. Доступен только тебе (пароль из env).

На том же сервере (Beget VPS) также хостится **мини-сайт**: лендинг с двумя приложениями, ссылки на RuStore, описание, политика конфиденциальности, пользовательское соглашение, условия подписки. Статические страницы, никаких дополнительных расходов.

> ⚠️ **Напомнить при реализации подписок:** попросить скинуть примеры файлов политики конфиденциальности и пользовательского соглашения для адаптации под проект.

**Раздел: Объявления**
- Создать объявление: тип (modal-баннер / push), текст, целевое приложение (Grammar8r / Words8r / оба)
- При создании modal: если есть активное (не истёкшее) → **предупреждение**: "Уже есть активное объявление, истекает через X ч Y мин. Всё равно создать?"
  - "Выйти" → отмена; "Всё равно создать" → оба активны, показываются по очереди (от старого к новому)
- Объявления перестают возвращаться через **24 часа** после создания (автоматически по `expires_at`)

**Раздел: Новости** 🔜 БЛИЖЕ К РЕЛИЗУ *(черновик, вид формы прорабатывается отдельно)*

Одна форма отправки, три канала галочками — **любая комбинация, полная свобода**:

- ☑ **Новость** — заголовок, тело, картинка (опционально). Живёт ~2 месяца.
- ☑ **Modal-баннер** — свой короткий текст + картинка (опционально). TTL 24 ч.
- ☑ **Push** — своя строка. ⚠️ Отправляется **один раз и навсегда**, отозвать нельзя —
  форма перед отправкой предупреждает об этом отдельно.

Тексты у каналов **раздельные** (у каждого своя длина и природа), общая только запись в истории.
Отправить можно и один канал: только баннер (техработы), только пуш («стрик горит»),
только новость (мелочь, будить незачем).

Дальше по постам: **список** всех (включая истёкшие), **редактирование** (текст, картинка —
пуш при этом НЕ переотправляется), **удаление** (исчезает из ленты у всех).

⚠️ Персональные баннеры пользователю (бан, предупреждение, апелляция) остаются там, где и были —
в карточке пользователя (`/admin/users/{uid}/notify`). Сюда не переезжают: там адресат один,
здесь рассылка всем.

**Раздел: Пользователи**

Список всех зарегистрированных пользователей. Колонки: UUID, email, есть ли подписка (тир + до когда), флаг `multi_device` ("Множество устройств" ⚠️), статус бана. Сортировка по флагу `multi_device`.

Карточка пользователя (по клику на UUID):
- Email, UUID (копируется), дата регистрации
- Подписка: тир, дата покупки, дата окончания
- Список устройств: device_id + first_seen + last_seen
- Флаг `multi_device` (выставляется автоматически если > 3 устройств)

**Действия в карточке пользователя:**
1. **Выдать подписку** → попап: выбор тира (Tier 1 / Tier 2) + количество часов → применяется сразу
2. **Suspend (мягкий бан)** → подписка аннулируется, пользователь видит Free-лимиты, контент доступен → опционально: поле для персонального сообщения пользователю (modal-баннер при следующем открытии приложения с текстом причины + "Если не согласен — напиши апелляцию на [email]")
3. **Ban (жёсткий бан)** → аккаунт заблокирован, при входе сообщение об ошибке → опционально: то же персональное сообщение с объяснением и адресом для апелляции
4. **Снять бан** → восстановить доступ
5. **Отправить персональное уведомление** → выбор типа (push / modal-баннер) + текст → только этому пользователю

Для suspend/ban указывается срок в часах (0 = бессрочно).

**Раздел: Промокоды**

Форма создания промокода:
- Скидка в % (1–99)
- Дата окончания (или бессрочно)
- Максимальное число применений
- Чекбокс **"Сгенерировать автоматически"** (по умолчанию включён):
  - Включён → поле кода неактивно, сервер генерирует `XXXX-NNNN` при сохранении
  - Выключен → поле кода становится активным, вводится вручную (минимум 5 символов, только латиница и цифры)

После создания — экран-подтверждение со всеми деталями:
> Промокод создан: **ABCD-1234**
> Скидка: 25%
> Действует до: 31 декабря 2026
> Максимум применений: 50
> Цена со скидкой: Tier 1 → 75₽ / Tier 2 → 134₽

Список промокодов — три фильтра: Активные / Неактивные / Все. Таблица:

| Код | Скидка | Истекает | Максимум | Использовано | Статус |
|-----|--------|----------|----------|--------------|--------|

Статус: `Активен` / `Истёк` (expires_at прошло) / `Исчерпан` (used_count == max_uses) / `Отключён` (is_active = false).
Действие: кнопка "Отключить" / "Включить" — меняет is_active без удаления.

Поток на клиенте: пользователь вводит промокод на экране подписки → `POST /promo/validate` → сервер возвращает скидку и итоговую цену → клиент отображает "Скидка 25%: было 179₽, стало 134₽" → при оплате клиент передаёт code в `POST /payment/create` → сервер инкрементирует `used_count` при получении вебхука об успешной оплате.

**Расчёт итоговой цены со скидкой:**
```
finalPrice = max(1, floor(basePrice * (1 - discountPercent / 100)))
```
Цена всегда целое число рублей, минимум 1₽. Копеек нет.
Пример: базовая 179₽, скидка 25% → floor(179 * 0.75) = 134₽. Скидка 99% → max(1, floor(179 * 0.01)) = 1₽.

**Раздел: Статистика**
- Активные пользователи за день / неделю / месяц
- AI-запросы в день (суммарно)
- Выручка (из YooKassa webhook)

**Раздел: Эндпоинты (только для панели, защищены admin-паролем)**

```
GET  /admin/users                    → список пользователей с флагами
GET  /admin/users/{uid}              → карточка пользователя
POST /admin/users/{uid}/subscription → { tier, hours } → выдать подписку
POST /admin/users/{uid}/suspend      → { hours, message? } → мягкий бан
POST /admin/users/{uid}/ban          → { hours, message? } → жёсткий бан
POST /admin/users/{uid}/unban        → снять бан
POST /admin/users/{uid}/notify       → { type: "push"|"modal", message } → персональное уведомление (без TTL, хранится в БД)
GET  /admin/users/{uid}/notifications → история персональных уведомлений пользователя (текст, дата, shown_at)
POST /admin/announcements            → { type, message, targetApp } → создать общее объявление (TTL 24ч)

# Новости (🔜 после релиза, см. раздел «Новости» ниже)
GET    /admin/news                   → список постов (включая истёкшие) для панели
POST   /admin/news                   → создать рассылку: пост + опционально баннер + опционально push
PATCH  /admin/news/{id}              → отредактировать пост (текст / картинка) — push НЕ переотправляется
DELETE /admin/news/{id}              → удалить пост (исчезает из ленты у всех)
GET  /admin/stats                    → агрегированная статистика
GET  /admin/promo                    → список всех промокодов (фильтр: active | inactive | all)
POST /admin/promo                    → { discountPercent, expiresAt?, maxUses } → создать промокод, вернуть сгенерированный code
PATCH /admin/promo/{code}/toggle     → включить / отключить промокод (is_active)

# Клиентский эндпоинт (вызывается приложением после показа modal)
POST /announcements/{id}/seen        → записать shown_at = now для этого объявления
```

---

## Хранилище промтов (server-side)

Промты AI-упражнений не хранятся в клиенте — только на сервере. Ключ = `AiExercise.id` клиента.

> ⚠️ **Базовый системный промт** — перед `system_prompt` каждого упражнения сервер
> автоматически подставляет базовый промт. Он составной: набор блоков зависит от **пути
> запроса** (`/generate` vs `/evaluate`) и двух условий — prefix `basics_` у `exercise_id`
> и наличие `words[]`. На пути **`/evaluate`** сервер дополнительно подставляет блок
> **«Обратная связь»** (правила «объясняй логику ошибки, а не просто факт»), а блок «Слова»
> не подставляет. Полное описание блоков и обе таблицы сборки —
> [phase3/ai_base_prompt.md](../phase3/ai_base_prompt.md).
>
> Итоговый system prompt для модели (DeepSeek) = `базовый промт` + `\n\n` + `system_prompt из таблицы`.
> ⚠️ **Порядок ради кэша DeepSeek:** неизменное — первым (базовый промт → промт карточки), изменяемое
> (слова пользователя, задание, ответ) — в конец. Тогда ~3 500 из ~4 500 входных токенов идут как повтор
> из кэша почти бесплатно — самый сильный рычаг цены (`subscription.md` → «Расчёт расходов»).

### Таблица `ai_exercise_prompts`

```sql
CREATE TABLE ai_exercise_prompts (
    exercise_id       VARCHAR(100) PRIMARY KEY,
    -- Связующий ключ = AiExercise.id клиента ("present_simple_card2_ex1").
    -- По нему сервер находит всё остальное при запросе POST /ai/exercise.

    system_prompt     TEXT        NOT NULL,
    -- Системный промт: правила для AI — как себя вести, как оценивать,
    -- в каком JSON-формате отвечать. У каждого упражнения свой.
    -- Пример: "Ты преподаватель английского. Оценивай строго.
    --          Отвечай в JSON: {isCorrect, correctedAnswer, feedback}"

    user_prompt       TEXT        NOT NULL,
    -- Шаблон задания. Сервер подставляет плейсхолдеры перед отправкой в модель:
    --   {{words}} ← слова пользователя из тела запроса (клиент собирает сам по WordSource)
    -- Приложение не собирает промт — только присылает сырые данные.
    -- Пример: "Слова: {{words}}.
    --          Дай русское предложение для перевода в Present Simple."

    ai_config_profile VARCHAR(50) NOT NULL
    -- Профиль настроек AI: имя из AiConfigProfile enum ("EXERCISE_LIGHT", "EXERCISE_HEAVY_300" и т.д.).
    -- Сервер смотрит на это поле и применяет нужные maxTokens и temperature из конфига профилей.
    -- Клиент это поле не шлёт — сервер сам знает по exercise_id.
);
```

Обновление промта — один `UPDATE` в БД, без релиза приложения.

---

## Доставка контента с сервера (теория + упражнения)

> 📄 **Детальная спека вынесена в [phase4_content_delivery.md](phase4_content_delivery.md)** —
> Object Storage (+ CDN опц.), статические JSON `version`/`tree`/`microtopic-{id}`, РФ-хостинг,
> заливка из конвейера, стоимость, гейты. Ниже — краткий исходный набросок (раздел сохранён для
> контекста, источник правды — отдельный файл).

> ⚠️ **ПРЕДВАРИТЕЛЬНЫЙ ПЛАН — может измениться.** Это набросок направления, а не финальная спека.
> Детали (формат ответов, гранулярность кэша, TTL) уточняются при реализации.

**Сейчас этого НЕ делаем.** Контент (дерево тем/микротем, карточки, упражнения) лежит в
бандленном `content.db` (read-only, собирается из MD → JSON → DB). Текущая локальная схема нужна,
чтобы **обкатать таблицы, парсинг контента и весь конвейер** на реальном материале. Переезд на
сервер — задача Фазы 4, и в наших реалиях он не сложный (см. ниже почему).

**Главный плюс, который делает переезд дешёвым:** у всех сущностей контента — **разные стабильные
ID** (ведём в `_id-registry.md`). Прогресс пользователя (`user.db`) ссылается именно на эти ID.
Пока ID стабильны и не переиспользуются — контент можно перенести на сервер, не трогая прогресс:
он снова сматчится по тем же ID. Это единственное, что важно соблюдать уже сейчас (и уже
соблюдается). Само поле версии контента и т.п. — добавляются позже дешёвой миграцией
`content.db`, заранее закладывать не нужно.

**Модель обновления — один глобальный номер версии контента:**
1. Приложение раз в день при первом запуске спрашивает у сервера версию контента.
2. Версия совпадает с локальной → ничего не делаем.
3. Не совпадает → сбрасываем **только кэш контента** и тянем заново.

**Две жёсткие оговорки (чтобы не выстрелить в ногу):**
- **`user.db` (прогресс) при сбросе НЕ трогаем никогда.** Сбрасывается только кэш контента.
  Безопасность переезда держится на стабильности ID (см. выше).
- **Не качать весь курс разом.** При смене версии: сбросили кэш → сразу подтянули **лёгкое дерево**
  тем/микротем, а **карточки — лениво по входу в микротему** (с TTL). Один глобальный номер версии
  + ленивая дозагрузка = и просто, и трафик не жрёт.

Темп обновлений под это и заточен: фиксы копятся, выкатываются пачкой (бамп версии на боевом
сервере) — поначалу правок много, дальше редко.

**Зачем это вообще (помимо «потом»):**
- обновлять контент без релиза приложения (как уже сделано для AI-промтов);
- меньше вес APK;
- сложнее выдрать контент (бандленный SQLite распаковывается тривиально) — планку поднимает,
  абсолютной защиты не даёт.

**Эндпоинты (набросок):**
```
GET /content/version
  → { version: <int|hash> }
  Клиент сверяет с локальной версией раз в день при старте.

GET /content/tree
  → дерево: категории → темы → микротемы (id, заголовки, порядок, флаги). Без карточек.
  Тянется при смене версии.

GET /content/microtopic/{id}
  → карточки + упражнения микротемы. Тянется лениво при входе, кэшируется с TTL.
```

---

## Поток AI-запроса

### POST /ai/exercise/generate

```
1. Парсинг авторизации:
   - Есть accessToken → верифицировать подпись (без DB) → читаем uid и tier прямо из токена
   - Нет accessToken → X-Device-Id → uid = deviceId, tier = "free" принудительно

1.5. Проверка бана (единственный запрос к БД на этом шаге):
   - users[uid].ban_status == "ban" → вернуть 403 { error: "account_banned" }
   - users[uid].ban_status == "suspend" → tier = "free" принудительно (продолжаем)
   - Обновить user_devices: сохранить device_id + uid + last_seen
   - Если уникальных device_id для uid > 3 → users[uid].multi_device = true

2. Если tier = "admin" → пропустить шаги 3–4 полностью, перейти к шагу 5.

3. Определить лимит по тиру (из кэша subscription_limits, TTL ~5 мин):
   - free: ai_per_day из subscription_limits
   - tier1/tier2/tester: ai_per_day + streak_bonus_requests

4. Проверить + инкрементировать лимит (АТОМАРНО, один SQL):
   - daily_ai_requests[uid, date_msk]
   - Если count >= limit → вернуть 429 { error: "limit_exceeded" }
   - Иначе → count++

5. Найти промт в ai_exercise_prompts по exerciseId:
   - Не найден → { error: "prompt_not_found" }
   - Подставить {{words}} из тела запроса

6. Сформировать запрос к DeepSeek:
   - System: system_prompt из таблицы
   - User: user_prompt с подставленными плейсхолдерами
   - temperature и max_tokens берутся из ai_config_profile записи в таблице
   - response_format: plain text (задание для пользователя)

7. POST → DeepSeek API (ключ только на сервере, никогда в APK)

8. Валидация ответа:
   - Непустая строка ≤ 2000 символов
   - Если невалидно → { error: "ai_error" }

9. Вернуть клиенту:
   { taskText, aiRequestsToday, aiDailyLimit }
```

### POST /ai/exercise/evaluate

```
1. Парсинг авторизации + проверка бана (те же шаги 1–1.5 что выше)

2. Санитизация входа:
   - userAnswer: обрезать до 500 символов, убрать управляющие символы
   - taskText: обрезать до 2000 символов, убрать управляющие символы

3. Найти промт в ai_exercise_prompts по exerciseId:
   - Не найден → { error: "prompt_not_found" }

4. Сформировать оценочный запрос к DeepSeek:
   - System: базовый промт (таблица «Оценка ответа», с блоком «Обратная связь»)
     + системный промт оценки (из ai_exercise_prompts или общий оценочный)
   - User:
       ЗАДАНИЕ: { taskText }
       ОТВЕТ ПОЛЬЗОВАТЕЛЯ: { userAnswer }
   - Метки ЗАДАНИЕ/ОТВЕТ ПОЛЬЗОВАТЕЛЯ защищают от prompt injection
   - response_format: Structured Outputs strict: true
     { isCorrect: Boolean, correctedAnswer: String, feedback: String }
     ⚠️ Structured Outputs `strict: true` — фича OpenAI. У DeepSeek есть JSON-режим, но без гарантии
     схемы: серверная валидация (п. 6) и один ретрай при кривом JSON становятся ОБЯЗАТЕЛЬНЫМИ.
     Перепроектировать этот шаг при старте ИИ-части (`phase4_infra_draft.md` п. 12,
     `ai_evaluation_draft.md`).

5. POST → DeepSeek API

6. Валидация ответа:
   - isCorrect: строго Boolean
   - correctedAnswer: непустая строка
   - feedback: непустая строка; потолок длины согласовать с потолком ответа ~800 токенов (подобрать на тестах)
   - Если невалидно → { error: "ai_error" }
   - `isCorrect` сервер пишет в свою статистику по `ai_exercise.id` (сколько верных / неверных) — для
     качества контента: если по одному id у всех подряд «неверно», сломан промт. Пользователю эта
     статистика не показывается (`grammar8r_plan.md` → «НИКАКОЙ ОЦЕНКИ ПОЛЬЗОВАТЕЛЮ В AI-ЗАДАНИЯХ»).

7. Обновить стрик — вызвать streak_logic(uid):
   - Та же функция что в POST /streak/activity
   - Результат НЕ включается в ответ — клиент получает его отдельно через параллельный POST /streak/activity

8. Вернуть клиенту:
   { isCorrect, correctedAnswer, feedback }
```

---

## Защита от манипуляций

| Угроза | Защита |
|--------|--------|
| API-ключ модели в APK | Ключ только на сервере, запросы через прокси |
| Превышение лимита | Атомарный инкремент в БД, проверка на сервере |
| Спуф deviceId | 5 запросов/день — низкая ценность, не критично |
| Prompt injection в userAnswer | Метка USER_ANSWER: + max 500 символов + system prompt |
| Кривой ответ от AI | Валидация на сервере + один ретрай, клиент не видит сырой ответ |
| Выпросить у AI баллы или оценку | Баллов в схеме нет вовсе — только `isCorrect` + фидбек |

---

## Схема БД

### users
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID PK | Уникальный ID пользователя |
| email | String? | Null если вход через Яндекс |
| password_hash | String? | bcrypt-хэш, null если вход через Яндекс. В открытом виде никогда не хранится. |
| yandex_uid | String? | Null если собственная регистрация |
| created_at | Timestamp | |
| ban_status | Enum | none / suspend / ban |
| ban_until | Timestamp? | Null = бессрочно. Для suspend/ban с ограничением по времени. |
| multi_device | Boolean | true если у аккаунта зафиксировано > 3 уникальных устройств. Выставляется автоматически. |

### user_devices
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID FK | |
| device_id | String | ID устройства (X-Device-Id) |
| first_seen | Timestamp | Когда устройство впервые связалось с аккаунтом |
| last_seen | Timestamp | Последний запрос с этого устройства |
Логика: при каждом запросе с JWT — сохраняем device_id + uid. Если уникальных device_id > 3 → `users.multi_device = true`.

### subscriptions
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID PK FK | |
| tier | Enum | free / tier1 / tier2 / admin / tester |
| expires | Timestamp? | Null = бессрочно (ручная выдача через панель) |
| purchased_at | Timestamp? | Когда куплена (null для ручной выдачи) |
| updated_at | Timestamp | |

### subscription_limits
Лимиты тиров. Читаются сервером с кэшем TTL ~5 минут — изменения вступают в силу без рестарта.

| Поле | Тип | Описание |
|------|-----|----------|
| tier | VARCHAR(20) PK | free / tier1 / tier2 / tester |
| ai_per_day | Int | AI-запросов в день. |
| microtopics_per_day | Int? | Микротем в день. Null = безлимит. |
| updated_at | Timestamp | Когда последний раз менялось. |

Начальные значения:

| tier | ai_per_day | microtopics_per_day |
|------|-----------|---------------------|
| free | 3 | 3 |
| tier1 | 30 | null |
| tier2 | 50 | null |
| tester | 30 | null |

### promo_codes
| Поле | Тип | Описание |
|------|-----|----------|
| code | VARCHAR(20) PK | Сам промокод (например SUMMER25). Генерируется случайно: 4 буквы + дефис + 4 цифры. |
| discount_percent | Int | Размер скидки в %, 1–99. |
| expires_at | Timestamp? | Null = бессрочно. |
| max_uses | Int | Максимальное количество применений. |
| used_count | Int | Сколько раз уже применён (инкрементируется при успешной оплате через вебхук YooKassa). |
| created_at | Timestamp | |
| is_active | Boolean | Ручное отключение из панели без удаления. |

**Логика генерации кода:** сервер генерирует `XXXX-NNNN` (4 случайных заглавных буквы + дефис + 4 случайных цифры), проверяет уникальность в БД, при коллизии генерирует заново.

### refresh_tokens
| Поле | Тип | Описание |
|------|-----|----------|
| token | String PK | Сам refresh token (случайный UUID или подписанная строка) |
| uid | UUID FK | Какому пользователю принадлежит |
| expires_at | Timestamp | Когда истекает (30 дней с момента выдачи) |
| created_at | Timestamp | |
Логика: при /auth/refresh — старый токен удаляется, выдаётся новый (ротация). Истёкшие токены можно чистить по крону.

### daily_ai_requests
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID | |
| date_msk | Date | Дата по МСК (UTC+3) |
| count | Int | Количество запросов за день |
| PRIMARY KEY | (uid, date_msk) | |

### ai_dialog_sessions

> Хранилище сессий режима практики «Диалог» (см. `practice_plan.md` → «Режим: Диалог» и раздел «AI-диалог» выше — там же поток обмена и формат JSON).

| Поле | Тип | Описание |
|------|-----|----------|
| session_id | UUID PK | |
| uid | UUID FK | |
| difficulty | Enum | simple / medium / hard |
| system_prompt | TEXT | Системный промт, сформирован и зафиксирован при старте: уровень сложности + срез слов пользователя. Не пересчитывается и не запрашивается у клиента повторно ни на одном следующем обмене — экономит токены и трафик |
| history | TEXT (JSON) | Массив реплик `[{ role: "user"\|"assistant", content, feedback? }, ...]`, накапливается с каждым обменом |
| turn_count | Int | Счётчик завершённых обменов, максимум 5 — на 5-м диалог завершается сводкой |
| created_at | Timestamp | |
| expires_at | Timestamp | `created_at + 1 час`. По истечении сессия удаляется фоновой задачей без сводки — пользователь её бросил |

Логика очистки: фоновая задача удаляет записи где `expires_at < now`, периодичность — раз в несколько минут.
Удаление также происходит мгновенно по `POST /ai/dialog/end` (кнопка [Завершить диалог]) и по достижении `turn_count == 5` (после отдачи финальной сводки).

### daily_microtopics
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID | |
| date_msk | Date | |
| count | Int | Пройдено микротем за день |
| PRIMARY KEY | (uid, date_msk) | |

### streaks
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID PK FK | |
| current_streak | Int | Текущий стрик (дней подряд) |
| last_activity_msk | Date | Последний день с засчитанным действием (из любого приложения) |
| last_activity_source | Enum | grammar8r / words8r — какое приложение первым засчитало сегодня |
| bonus_requests | Int | Текущие бонусные запросы/день (только Grammar8r AI-лимиты) |
| freezes_used | Int | Заморозок использовано в текущем месяце |
| freeze_month | String | "2026-04" — для сброса заморозок |

### jwt_secrets
Секрет для подписи access token хранится в env переменной (`JWT_SECRET`), не в БД.
Refresh token — случайный UUID, подпись не нужна, валидность проверяется по наличию в таблице `refresh_tokens`.

### fcm_tokens
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID FK | |
| token | String | FCM registration token устройства |
| app | Enum | grammar8r / words8r |
| updated_at | Timestamp | Обновляется при каждом запуске приложения |

### announcements
| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID PK | |
| message | String | Текст объявления |
| type | Enum | modal / push |
| target_app | Enum | grammar8r / words8r / all |
| uid | UUID? FK | Null = общее (всем). Заполнено = персональное (только этому пользователю). |
| is_personal | Boolean | true = персональное. Дублирует uid != null для удобства запросов. |
| created_at | Timestamp | |
| expires_at | Timestamp? | Для общих: created_at + 24 часа. Для персональных: **null** (без срока, хранится вечно). |
| shown_at | Timestamp? | Когда пользователь увидел (приложение сообщает). Null = ещё не показано. |

**Логика на клиенте при открытии приложения:**
```
GET /announcements → список активных
1. Сначала показать персональные (is_personal = true, shown_at = null) — по одному, от старого к новому
2. После всех персональных — показать общие (is_personal = false, в рамках TTL 24ч)
После показа каждого modal → POST /announcements/{id}/seen → сервер записывает shown_at = now
```

**Персональные modal-уведомления хранятся в БД навсегда** — доступны в истории профиля пользователя в панели.

### news_posts 🔜 БЛИЖЕ К РЕЛИЗУ (черновая схема, поля ещё поплывут)
| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID PK | |
| title | String | Заголовок поста (он же строка в ленте) |
| body | Text | Тело поста, длина не ограничена |
| image_url | String? | Картинка в объектном хранилище. Null = пост без картинки. |
| target_app | Enum | grammar8r / words8r / all |
| created_at | Timestamp | |
| updated_at | Timestamp | Меняется при редактировании; клиент по нему обновляет кэш. |
| expires_at | Timestamp | created_at + 60 дней. После — не отдаётся клиенту. |
| push_sent_at | Timestamp? | Когда по этому посту ушёл push. Null = слали без пуша. Только для истории в панели — повторно пуш не отправляется никогда. |

Прочитанность постов на сервере **не хранится** — она чисто клиентская (DataStore).
Связь с баннером: у `announcements` появляется `news_post_id UUID? FK` — заполнен, если баннер
создан вместе с постом (даёт баннеру кнопку «Подробнее» → лента).

### user_progress
| Поле | Тип | Описание |
|------|-----|----------|
| uid | UUID FK | |
| event_type | Enum | card_completed / exercise_done |
| entity_id | Int | id карточки или упражнения |
| occurred_at | Timestamp | Время события (клиентское, MSK) |

---

## Стрик — серверная логика

Общий стрик для Grammar8r и Words8r. Засчитывается первое квалифицирующее действие за день из любого приложения.

**Квалифицирующие действия:**
- Grammar8r: любой AI-запрос (упражнение, уточнение)
- Words8r: завершена сессия повторения / игра Wordly / игра Maze / выучено 5 новых слов

```python
# streak_logic(uid) — вызывается из /streak/activity И из /ai/exercise
def streak_logic(uid) -> StreakResult:
    today = current_date_msk()
    streak = db.get_streak(uid)
    tier = db.get_tier(uid)

    # Уже засчитано сегодня (из любого приложения)
    if streak.last_activity_msk == today:
        return StreakResult(
            streakIncremented=False,
            currentStreak=streak.current_streak,
            daysToNextMilestone=days_to_next(streak.current_streak),
            nextMilestone=next_milestone(streak.current_streak),
            nextMilestoneBonus=bonus_label(tier, next_milestone(streak.current_streak))
        )

    milestone_reached = None

    if streak.last_activity_msk == today - 1:
        streak.current_streak += 1          # продолжаем серию
    elif streak.last_activity_msk == today - 2 and freeze_available(uid):
        streak.freezes_used += 1            # заморозка — стрик сохраняется
    else:
        streak.current_streak = 1           # сброс
        streak.bonus_requests = 0

    streak.last_activity_msk = today

    # Проверить milestone
    milestone_reached = check_milestone(uid, streak.current_streak, tier)

    db.save_streak(uid, streak)

    return StreakResult(
        streakIncremented=True,
        currentStreak=streak.current_streak,
        daysToNextMilestone=days_to_next(streak.current_streak),
        nextMilestone=next_milestone(streak.current_streak),
        nextMilestoneBonus=bonus_label(tier, next_milestone(streak.current_streak)),
        milestoneReached=milestone_reached  # None если не достигнут
    )


def days_to_next(current_streak) -> Int:
    for m in [7, 20, 30, 60, 100]:
        if current_streak < m:
            return m - current_streak
    return 0  # все milestones пройдены

def next_milestone(current_streak) -> Int:
    for m in [7, 20, 30, 60, 100]:
        if current_streak < m:
            return m
    return 100  # уже за 100
```

**Атомарность:** `streak_logic` выполняется в одной транзакции БД — нет race condition если Grammar8r и Words8r шлют запросы одновременно.

### Milestone-бонусы (серверная таблица)

```
MILESTONES = [7, 20, 30, 60, 100]

# Прирост за шаг (кумулятивно даёт итог из subscription.md):
BONUS_TABLE = {
    "free":  { 7: 1, 20: 0, 30: 1, 60: 0, 100: 0 },  # max +2  → итого 7/день
    "tier1": { 7: 1, 20: 1, 30: 2, 60: 2, 100: 2 },  # max +8  → итого 28/день
    "tier2": { 7: 2, 20: 2, 30: 2, 60: 3, 100: 3 },  # max +12 → итого 47/день
}

def check_milestone(uid, streak, tier):
    for milestone in MILESTONES:
        if streak == milestone:
            bonus = BONUS_TABLE[tier][milestone]
            streak.bonus_requests += bonus
            # Для Free: bonus_requests не может превышать 2
            if tier == "free":
                streak.bonus_requests = min(streak.bonus_requests, 2)
```

---

## Сброс лимитов

- AI-запросы и микротемы: сбрасываются через `date_msk` — новая дата = новые строки
- Заморозки: `freeze_month` != текущий месяц → `freezes_used = 0`, обновить `freeze_month`
- Бонусные запросы: сгорают при сбросе стрика (streak.bonus_requests = 0)

---

## Конфигурация (env переменные)

```
DEEPSEEK_API_KEY=sk-...
JWT_SECRET=...
DATABASE_URL=postgresql://...
YOOKASSA_SECRET_KEY=...
YOOKASSA_SHOP_ID=...
YANDEX_CLIENT_ID=...       # для верификации Яндекс токенов
FIREBASE_SA_JSON=...       # путь/содержимое JSON сервис-аккаунта Firebase — отправка push
                           # ⚠️ FCM HTTP v1 + OAuth. Старый FIREBASE_SERVER_KEY (легаси-API
                           # с серверным ключом) Google отключил — этот путь больше не работает.
ADMIN_PASSWORD=...         # пароль веб-панели администратора
```

---

## Порядок реализации

- [ ] **S1** Spring Boot (Java) проект, Docker, деплой на Beget VPS через GitHub Actions; сразу же — kotlinx-конвертер
      для DTO из `grammar-shared` + тест на `@SerialName` (см. «Шероховатости» выше)
- [ ] **S2** PostgreSQL подключение, схема БД (миграции через Flyway или Exposed)
- [ ] **S3** POST /auth/register + /auth/login (email+пароль, bcrypt, accessToken + refreshToken)
- [ ] **S3a** POST /auth/refresh (ротация refresh token, актуальный tier из БД в новый accessToken)
- [ ] **S4** POST /auth/yandex (верификация Яндекс-токена, те же два токена на выходе)
- [ ] **S5** GET /subscription — возвращает тир, лимиты, стрик
- [ ] **S6** POST /ai/exercise — полный поток: лимит → DeepSeek → валидация → стрик
- [ ] **S7** POST /ai/clarification — уточнение по карточке
- [ ] **S8** POST /limits/microtopic — проверка и трекинг микротем
- [ ] **S9** POST /webhook/yookassa — обработка оплаты
- [ ] **S10** Консольная команда ручной выдачи подписки (без изменений кода)
- [ ] **S11** Интеграция YooKassa SDK в приложении
- [ ] **S12** Таблица `user_devices`: трекинг device_id → uid, автовыставление флага `multi_device` при > 3 устройствах
- [ ] **S13** Поля `ban_status` / `ban_until` в таблице users: suspend/ban логика в потоке каждого запроса
- [ ] **S14** FCM интеграция (**FCM HTTP v1 + сервис-аккаунт**, не легаси server key): регистрация токенов (`POST /fcm/register`), отправка push из панели (всем / одному), тест-отправка на своё устройство, лог отправок, автоудаление мёртвых токенов, каналы уведомлений. См. раздел «Push-уведомления: как доставляются»
- [ ] **S14b** POST /streak/activity — общий стрик для Grammar8r и Words8r: принимает источник + тип действия, возвращает данные для баннера. Атомарная транзакция.
- [ ] **S15** POST /progress/sync + GET /progress — синк прогресса по событию
- [ ] **S16** GET /announcements — активные объявления с фильтром по TTL 24ч
- [ ] **S17** Веб-панель: список пользователей с флагами, карточка пользователя, выдача подписки (тир + часы), suspend/ban/unban с персональным сообщением, персональные уведомления, создание объявлений, статистика
- [ ] **S18** Добавить FCM в оба приложения (Grammar8r + Words8r): регистрация токена при старте, обработка входящих push
- [ ] **S19** Мини-сайт на том же сервере: лендинг + ссылки на RuStore + страницы с правовыми документами (политика конфиденциальности, пользовательское соглашение, условия подписки)
- [ ] **S20** Таблица `subscription_limits`: начальные значения для free/tier1/tier2/tester, серверный кэш с TTL ~5 мин, использование в `/ai/exercise` и `/tier-limits`
- [ ] **S21** `GET /tier-limits` — публичный эндпоинт, читает из кэша subscription_limits
- [ ] **S22** Промокоды: таблица `promo_codes`, `POST /promo/validate`, вебхук YooKassa инкрементирует used_count, панель: создание / список / toggle
- [ ] **S23** *(предварительно)* Доставка контента с сервера: `GET /content/version` + `/content/tree` + `/content/microtopic/{id}`, глобальная версия с ежедневной сверкой, сброс кэша контента (не `user.db`), ленивая дозагрузка карточек. См. раздел «Доставка контента с сервера»
- [ ] **S24** *(предварительно, ближе к релизу)* Новости: таблица `news_posts`, `GET /news`, CRUD в панели, отправка рассылки одной формой (пост + опционально баннер + опционально push), картинки в объектном хранилище. Идёт ПОСЛЕ доставки контента (S23) — без неё фича теряет главный смысл. См. раздел «Новости»
- [ ] **S25** *(предварительно, ближе к релизу)* Векторные анимации: раздача Lottie-файлов из объектного хранилища, `animations/catalog.json` со слотами и четырьмя условиями (weight / from-to / minAppVersion), раздел панели «Анимации» (сводка по слотам, загрузка с предпросмотром в нашей раме, привязка к слоту, замена новым файлом, снятие, удаление), снятие анимации с раздачи с корректным фолбэком и уборкой на клиенте. См. раздел «Векторные анимации»